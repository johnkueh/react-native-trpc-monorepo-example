import { ScreenContainer } from '../features/design-system/layouts';
import { AddGreetingForm } from '../features/greetings/AddGreetingForm';

export default function AddGreetingScreen() {
  return (
    <ScreenContainer>
      <AddGreetingForm />
    </ScreenContainer>
  );
}
